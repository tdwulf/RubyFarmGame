require_relative 'plants'

array_plants
plant = 'Coffee'


@plants.each do |x, name, cost, exp|
        if name == plant
        pcost = cost
        puts pcost
        puts name
        else
        end
    end

